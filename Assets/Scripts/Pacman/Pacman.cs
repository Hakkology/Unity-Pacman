using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pacman : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("sELAM BEN PACMAN");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
